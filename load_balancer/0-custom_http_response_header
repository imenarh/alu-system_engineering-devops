#!/usr/bin/env bash
# Configures Nginx with a custom X-Served-By response header
# shellcheck disable=SC2154

apt update -y
apt install nginx -y


sed -i '/server_name _;/a\\tadd_header X-Served-By $hostname;' /etc/nginx/sites-available/default

service nginx restart